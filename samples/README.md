# Unstructured polar-cap sample for compliance-checker issue #1293

File: `sample_unstructured.nc` (2.6 MB)
- FESOM2 CORE2 mesh, 126 858 nodes
- `lat(nod2)`, `lon(nod2)` as CF auxiliary coordinates
- `lat_bnds(nod2, 18)`, `lon_bnds(nod2, 18)` polygonal cell bounds
- Exposes the polar-cap edge case discussed in https://github.com/ioos/compliance-checker/issues/1293
